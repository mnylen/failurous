.data-box.fail{:class => fail_css_class(fail)}
  .box-header
    .box-title
      .title=fail.title
      .location
        %strong at
        =fail.location
    .box-right
      -unless fail.acknowledged
        %span.last_occured_at
          %span.date=time_ago_in_words(fail.last_occurence_at) + " ago"
        %span.actions
          =link_to('Resolve', ack_project_fail_path(fail.project, fail), :class => 'button positive')
        %span.count.negative
          %span.number= fail.occurence_count
      -else
        .state [resolved]

  =render :partial => 'fails/occurence', :object => fail.last_occurence
